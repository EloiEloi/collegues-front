class Photo {
    matricule: string;
    photoUrl: string;

    constructor(matricule: string, photoUrl: string) {
        this.matricule = matricule;
        this.photoUrl = photoUrl;
    }
}
export { Photo };